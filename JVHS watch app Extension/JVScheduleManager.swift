//
//  JVScheduleManager.swift
//  JVHS
//
//  Created by Kevin Turner on 4/30/16.
//  Copyright © 2016 Kevin Turner. All rights reserved.
//

import Foundation

class JVScheduleManager: NSObject {
/*
    static func currentClassPeriod() -> String
    {
        let date = NSDate()
        let formatter  = NSDateFormatter()
        formatter.dateFormat = "yyyy-MM-dd"
        let todayDate = formatter.dateFromString(formatter.stringFromDate(date))!
        let myCalendar = NSCalendar(calendarIdentifier: NSCalendarIdentifierGregorian)!
        let unitFlags: NSCalendarUnit = [.Hour, .Weekday, .Minute]
        let myComponents = myCalendar.components(unitFlags, fromDate: todayDate)
        let weekDay = myComponents.weekday
        let hour = myComponents.hour
        let minutes = myComponents.minute
        
        if weekDay == 1 || weekDay == 2 || weekDay == 5 {
            
            
        } else if weekDay == 3 {
            
        } else {
            
        }
        
        return ""
    }
 */
    
}
